# Overview of the data structures used

## Locked dependencies

```
locked_dependencies: Dict[Tuple[str, str], Tuple[Optiona [TemporalDependency], Optional[ExistentialDependency]]]
```

## Adjacency matrix
